package dev.piko.desktop.ui.player.controls

/**
 * 画面比例选项。
 *
 * 与 shared 中 dev.piko.shared.media.player.PlayerAspectRatio 同名同值；合并后删除本文件，
 * 改为导入 shared 的版本即可，控件代码不需要其他改动。
 */
enum class PlayerAspectRatio { Fit, Crop, Stretch }
